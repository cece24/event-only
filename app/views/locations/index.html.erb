<p><%= link_to "New Location", new_location_path %></p>

<% @locations.each do |current_location| %>
  <%= render 'single_location', location: current_location, display_location_name: true %>
  <%= render 'location_form', location: current_location %>
<% end %>
